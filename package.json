{
  "name": "change-name",
  "version": "0.3.0",
  "description": "replace names in a folder",
  "bin": {
    "change-name": "index.js"
  },
  "license": "MIT",
  "dependencies": {
    "change-case": "^4.1.2",
    "commander": "^10.0.1",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "set-defaults": "^0.0.1"
  },
  "scripts": {
    "prepare": "set-defaults .vscode"
  }
}
